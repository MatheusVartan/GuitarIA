version: '3.8'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: guitaria
      POSTGRES_USER: guitaria
      POSTGRES_PASSWORD: guitaria
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
